{
  "name": "channel-sample",
  "version": "0.0.1",
  "description": "Claude Code Channel server - delivers external HTTP requests and scheduled events to Claude Code sessions",
  "type": "module",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --watch run src/main.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "node-cron": "^4.2.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node-cron": "^3.0.11",
    "typescript": "^5.9.3"
  }
}
